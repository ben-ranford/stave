# Contributing

Stave is an architecture spike. Keep the core standard-library-only, preserve deterministic snapshots, and add tests for behavior changes. Run `gofmt -w *.go cmd/*/*.go`, `go vet ./...`, and `go test -race ./...` before proposing changes.
