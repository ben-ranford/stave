// Package stave is a focused architecture spike for adaptive, branded Go UI
// primitives that expose one semantic model to human and agent interfaces.
//
// The spike proves provisional stable logical identity, generation-aware action
// targets, separate semantic roles and style intents, immutable application
// themes, deterministic headless snapshots and reducer replay, and
// capability-driven plain rendering. It intentionally does not claim the
// production stave-node-id-v1 contract or implement a terminal event loop,
// asynchronous effects, secret fields, or the complete JSON-RPC transport.
package stave
