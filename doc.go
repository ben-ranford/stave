// Package stave is the production composition surface for adaptive, branded,
// agent-compatible Go interfaces. Applications define one pure reducer, one
// semantic view, one action registry, and one theme; human and machine runtimes
// consume the same immutable session state.
package stave
